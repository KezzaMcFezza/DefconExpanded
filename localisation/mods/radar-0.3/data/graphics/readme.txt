Directory content:
blur.bmp: the actual file for defcon.
blur.xcf: the gimp source file.
source.xcf: all the data from defcon, ready to be treated by the script
ranges.scm: gimp script-fu script that takes a layer with a territory and
            transforms it into three layers with the ranges. The image needs
            to be scaled up to 1024xsomething before.
